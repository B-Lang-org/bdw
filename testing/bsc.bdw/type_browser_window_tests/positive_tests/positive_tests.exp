set here [file join [absolute $srcdir] $subdir]
source $here/../../bdw.tcl

if { [info exists env(DISPLAY)] && $env(DISPLAY) != "" } {

# Checks the Types menu functionality
bdw_run_compare_pass types_menu_test.tcl

}
 
