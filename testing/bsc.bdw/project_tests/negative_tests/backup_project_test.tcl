
# Backup the project
handlers::backup_project project.tgz -input_files

# Backup the project
handlers::backup_project

# Opens the specified project
handlers::open_project project.bspec

# Backup the project
handlers::backup_project project.tgz -input_fis

# Backup the project
handlers::backup_project project.tgz -project_d

# Backup the project
handlers::backup_project project.tgz -search_pat

# Backup the project
handlers::backup_project project.tgz -input_fies -poject_dir

# Backup the project
handlers::backup_project

# Backup the project
handlers::backup_project project.tgz

# Backup the project
handlers::backup_project project.tgz -options -v

# Backup the project
handlers::backup_project -options -v

# Backup the project
handlers::backup_project project.tgz -input_files -project_dir -search_path -options

# Backup the project
handlers::backup_project project.tgz -input_files -project_dir -search_path -options -v -search_path_fles

# Backup the project
handlers::backup_project project.tgz -input_files -project_dir -search_path -options -v -search_path_fles

# Closes the project
handlers::close_project

# Exits the workstation
exit
