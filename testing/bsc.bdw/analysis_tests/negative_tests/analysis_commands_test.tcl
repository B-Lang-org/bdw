# Opens the specified project
handlers::open_project project.bspec

handlers::load_package

handlers::load_package Mult5

handlers::import_hierarchy

handlers::import_hierarchy Mult4Tb

handlers::reload_packages

handlers::add_type

handlers::load_module

handlers::load_module mkMult5

handlers::reload_module

handlers::show_schedule

handlers::show_schedule mkMult5

# Closes the project
handlers::close_project

# Exits the workstation
exit
